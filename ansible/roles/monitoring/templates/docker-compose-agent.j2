version: '2'
services:
  zabbix-agent:
    container_name: zabbix-agent
    image: zabbix/zabbix-agent:alpine-3.2-latest
    restart: always
    ports:
     - "10050:10050"
    environment:
     - ZBX_HOSTNAME={{monitoring_agent_host}}
     - ZBX_SERVER_HOST={{monitoring_server_host}}
